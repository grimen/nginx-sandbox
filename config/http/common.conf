# directory listing
# autoindex on;

# http://wiki.nginx.org/HttpCoreModule#sendfile
#
sendfile off; # OSX: disable

# http://wiki.nginx.org/HttpCoreModule#if_modified_since
#
if_modified_since exact; # off | *exact | before

# http://nginx.org/en/docs/http/ngx_http_core_module.html#server_tokens
#
# Turn off Nginx version number displayed on all auto generated error pages.
#
server_tokens off;
